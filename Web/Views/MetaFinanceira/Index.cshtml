@model IEnumerable<MetaFinanceira>

<div class="contanier p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h4 class="text-white-65">Minhas Metas</h4>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="MetaFinanceira" asp-action="MostrarViewCadastrar" class="btn btn-info">Criar nova meta</a>
        </div>
    </div>
</div>

@* <table class="table table-striped table-hover">
    <thead>
        <tr class="table-secondary">
            <th>Nome</th>
            <th>Descrição</th>
            <th>Valor Meta</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null)
        {
            @foreach (var metafinanceira in Model)
            {
                <tr>
                    <td>@metafinanceira.Titulo</td>
                    <td>@metafinanceira.Descricao</td>
                    <td>@metafinanceira.ValorMeta</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="4">Não há dados para exibir.</td>
            </tr>
        }
    </tbody>
</table> *@

@foreach (var metafinanceira in Model)
{
    <tr>
        <div class="card border-secondary mb-3">
        <div class="card-body border-top border-info">
            <h4 class="card-title border-secondary "><td>@metafinanceira.Titulo</td></h4>
            <p class="card-text text-light"><td>@metafinanceira.Descricao</td></p>
            <h6 class="card-title text-white mb-1">
                <td>R$ @metafinanceira.ValorMeta
                    <div class="d-flex justify-content-end mb-0" role="group">
                        <a asp-controller="MetaFinanceira" asp-action="" asp-route-id="" class="btn btn-outline-primary mx-1"> <i class="bi bi-pencil-square"></i>Detalhes</a>
                        <a asp-controller="MetaFinanceira" asp-action="" asp-route-id="" class="btn btn-outline-warning mx-1"> <i class="bi bi-trash-fill"></i>Editar</a>
                        <a asp-controller="MetaFinanceira" asp-action="MostrarViewExcluir" asp-route-id="@metafinanceira.Id" class="btn btn-outline-danger mx-1"> <i class="bi bi-trash-fill"></i>Deletar</a>
                    </div>
                </td>
            </h6>
        </div>
        </div>
    </tr>
}

<script src="~/js/Pages/metafinanceira.js" asp-append-version="true"></script>
<script>
    metafinanceira.init({
        urls: {
            index: '@Url.Action("Index", "MetaFinanceira")'
        }
    });
</script>
